#!/bin/sh

# Handle custom keys for nsxiv
# The handler is invoked by pressing ctrl-x

set_wallpaper() {
    xwallpaper --zoom "$1"
    cp "$1" "$HOME/.local/share/wallpaper/wallpaper.png"
}

while read -r file
do
    case "$1" in
        "w") set_wallpaper "$file" ;;
    esac
done
