#!/bin/sh

# Custom alias and commands

# Core Utilities
alias df="df -hT"
alias du="du -hs"
alias cp="cp -iv"
alias mv="mv -iv"
alias rm="rm -v"
alias ip="ip -c -h"
alias free="free -hw"
alias grep="grep --color=auto"
alias ls="ls --color=auto --group-directories-first -AF"
alias ll="ls --color=auto -lhAF"

# Better cd
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'

# Packages manager
alias update="doas xbps-install -Syu && doas reboot"
alias install="doas xbps-install -Sy"
alias clean="doas xbps-remove -Oo"

# Git
alias ggg="git status"
alias gga="git add --verbose"
alias ggp="git push"
alias ggc="git commit --verbose"

# Commands
playmp3() {
    setsid -f \
        mpv --player-operation-mode=pseudo-gui \
            --shuffle --af=lavfi="acompressor" \
            --no-terminal -- ./*.mp3
}

mkd() {
    mkdir -pv "$1" && cd "$1" || exit 1
}

ggs() {
    local hash=$(git log --oneline | fzf | cut -d ' ' -f1)
    [ -n "$hash" ] && git show "$hash"
}

remove() {
    local package=$(xbps-query -l | cut -d ' ' -f2 | fzf)
    [ -n "$package" ] && doas xbps-remove "$package"
}
